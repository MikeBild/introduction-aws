{
  "private": true,
  "name": "todo-api",
  "version": "1.0.0",
  "description": "Todo API",
  "main": "build/server.js",
  "scripts": {
    "clear": "rimraf -r build .cache",
    "build": "npm run clear && npm run build:server && cp -R node_modules ./build",
    "build:server": "parcel build src/server.ts --target node -d build --no-cache",
    "start": "node build/server",
    "dev": "npm run clear && npm run build:server && npm run dev:watch",
    "dev:watch": "concurrently npm:watch:server npm:watch:build",
    "watch:server": "parcel watch src/server.ts --target node -d build",
    "watch:build": "nodemon --watch build build/server",
    "deploy": "cdk deploy"
  },
  "dependencies": {
    "athena-express": "^2.0.6",
    "aws-sdk": "^2.382.0",
    "aws-serverless-express": "^3.3.5",
    "cors": "^2.8.5",
    "express": "^4.16.4",
    "express-list-endpoints": "^4.0.0"
  },
  "devDependencies": {
    "@aws-cdk/aws-apigateway": "^0.21.0",
    "@aws-cdk/aws-glue": "^0.21.0",
    "@aws-cdk/aws-iam": "^0.21.0",
    "@aws-cdk/aws-lambda": "^0.21.0",
    "@aws-cdk/aws-s3": "^0.21.0",
    "@aws-cdk/cdk": "^0.21.0",
    "@types/aws-serverless-express": "^3.3.0",
    "@types/cors": "^2.8.4",
    "@types/express": "^4.16.0",
    "@types/node": "^10.12.18",
    "concurrently": "^4.1.0",
    "nodemon": "^1.18.9",
    "parcel": "^1.11.0",
    "rimraf": "^2.6.2",
    "typescript": "^3.2.2"
  }
}
